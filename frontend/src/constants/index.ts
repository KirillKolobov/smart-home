export const DEFAULT_REQUIRED_MSG = "This field is required"; 
export const DEFAULT_MAX_LENGTH_MSG = "Too many symbols";
export const DEFAULT_MIN_LENGTH_MSG = "Too few symbols";
export const PHONE_REGEX = /^\+7 \(\d{3}\) \d{3}-\d{2}-\d{2}$/;
export const EMAIL_REGEX = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
export const PASSWORD_REGEX = /^(?=.*[A-Za-z])(?=.*\d)[A-Za-z\d]{8,}$/; // Minimum eight characters, at least one letter and one number

